# Cursor Ignore File
# This file tells Cursor which files to ignore (won't be indexed or searchable)
# Note: We want Cursor to SEE .env files even though git ignores them

# Large directories
node_modules/
dist/
dist-ssr/

# Logs
*.log

# Large generated files
*.map

# Build artifacts
.cache/
.parcel-cache/

# Don't ignore .env files - we want Cursor to see them!
# (They're still in .gitignore for security)
!.env
!.env.*
!.env.local
!.env.development
!.env.production
!.env.test




